{
  "/ <-- you are here": {
    "/auth": [
      { "POST /login": "log into your account" },
      { "POST /register": "create an account" },
      { "POST /logout": "logout of your account" }
    ],
    "/deck": [
      { "POST /": "get all decks for user", "body": { "user_id": "number" } },
      { "GET /:deck_id": "get a specific deck with cards" },
      {
        "POST /new": "create deck",
        "body": {
          "name": "string",
          "user_id": "number"
        }
      },
      { "DELETE /:deck_id": "delete deck" },
      {
        "PUT /:deck_id": "update deck",
        "body": {
          "answer": "string",
          "description": "string",
          "name": "string"
        }
      }
    ],
    "/card": [
      { "GET /getbydeck/:deck_id": "get a specific card from a deck" },
      {
        "POST /:deck_id": "create a card for a deck",
        "body": {
          "question": "string",
          "answer": "string",
          "description": "string"
        }
      },
      {
        "PATCH /:deck_id/:card_id": "update a card from a deck",
        "body": {
          "question": "string",
          "answer": "string",
          "description": "string"
        }
      },
      {
        "DELETE /:card_id": "delete a card"
      }
    ]
  }
}
